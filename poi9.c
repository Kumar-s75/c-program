#include <stdio.h> 

int main(){
    void *ptr=NULL;
    int *p=NULL;
    printf("The size of  pointer is :%d\n",sizeof(ptr));
    printf("The value inside variable ptr is :\n%x",ptr);

     return 0;
}